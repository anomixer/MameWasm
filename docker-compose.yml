version: '3.8'

services:
  mame-build:
    build: .
    volumes:
      - .:/src
    working_dir: /src
    tty: true
    stdin_open: true
    # Example usage: docker-compose run mame-build mame-build TARGET=mame SUBTARGET=ample
